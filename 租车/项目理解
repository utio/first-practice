一些出现的问题和学习到的东西

使用到的知识点：
	继承、多态、对类的封装、JavaBean的使用、

一个项目需要那些类，这些类各自的作用分别是什么
	1. 如果有类似仓库的实体存在，建立一个Service类，用来建立仓库，存储数据，从这个类建立的仓库中提取货物给客户
	2. 如果有三行以上的代码重写超过两边，就要定义一个方法，将实现过程放入方法内
	3. 客户类，客户类中对需求进行实现，对本代码来说，就是租车行为的实现
	4. 每一类车计算价钱的方法不同，放入每一类车的子类中

花费时间最多的一点：在Service类中去区分车牌号是否相同，多态的使用不牢靠，第一时间使用的是比较去选出不同的元素，没有想到可以直接使用动态数组的方式，提一辆车删除一辆车

对equals方法的改写，使用能完全区分物体的属性进行修改，本例中使用的是车牌号用来区分不同汽车

多态的使用：在Service类中使用的多态方法getAuto，使用父类作为返回声明，返回子类，简化了思考返回类型的过程，使代码行为逻辑更通畅

